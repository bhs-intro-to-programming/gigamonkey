/*
 * Return a new array containing only those elements of the array argument for
 * which the predicate returns true.
 *
 * predicate: a predicate function.
 * array: an array of values
 */
const filter = (predicate, array) => {
  // implement
};

/*
 * Return a new array containing for each element in the array argument the
 * value returned by fn when called with that element.
 *
 * fn: a one-argument function
 * array: an array of values
 */
const map = (fn, array) => {
  // implement
};

/*
 * Return the value produced by repeatedly calling fn with the initialValue and
 * the next element of the array argument (for the first element) or the value
 * returned by the previous call to fn and the next element of the array.
 *
 * fn: a one-argument function
 * initialValue: the starting value for the reduction
 * array: an array of values
 */
const reduce = (fn, initialValue, array) => {
  // implement
};

/*
 * Return true if, and only if, the predicate function returns true for every
 * element of the array argument.
 *
 * predicate: a predicate function.
 * array: an array of values
 */
const every = (predicate, array) => {
  // implement
};

/*
 * Return true if, and only if, the predicate function returns true for some
 * element of the array argument.
 *
 * predicate: a predicate function.
 * array: an array of values
 */
const some = (predicate, array) => {
  // implement
};
