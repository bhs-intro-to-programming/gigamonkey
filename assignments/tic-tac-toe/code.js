// Note the lack of a declaration (a let or const) here. This variable has been
// defined in the framework but you can reassign it to a different function
// value to handle mouse clicks. This is just a place holder.
onclick = (x, y) => {
  console.log(`Click at ${x},${y}`);
};
