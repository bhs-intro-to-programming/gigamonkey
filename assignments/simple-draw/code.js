const headSize = 50;
drawFilledCircle(width / 2, headSize + 20, headSize, 'white');
const bodySize = headSize * 1.2;
drawFilledCircle(width / 2, headSize + 20 + headSize + bodySize, bodySize, 'white');